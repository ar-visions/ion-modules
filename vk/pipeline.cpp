#include <dx/dx.hpp>
#include <vk/vk.hpp>

///
/// i would actually like to NOT expose DescriptorSet at all
/// too many bindings have redundancy and it will make sense to contain this


Pipeline::Pipeline(Device *device) {
}

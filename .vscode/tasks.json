{
	"version": "2.0.0",
	"tasks": [
	  {
		"label": "build",
		"type": "shell-game",
		"options": {"cwd": "${workspaceFolder}/build"},
		"command": "make orbiter -j16",
		"presentation": {
		  "reveal": "silent",
		  "panel": "shared"
		}
	  }
	]
}